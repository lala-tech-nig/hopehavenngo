import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "Hope Haven Foundation",
  description: "Hope Haven Foundation is a compassionate non-profit organization dedicated to empowering widows, orphans, and the most vulnerable members of society. Founded on the principles of love, dignity, and social justice, our mission is to restore hope where it has been lost and to be a voice for those who are often unheard.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
